<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Home • My Blog</title>

  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
<%- include("partials/header") %>

<main class="container py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0">All Posts</h1>
    <a class="btn btn-primary" href="/compose">+ New Post</a>
  </div>

  <% if (!posts || posts.length === 0) { %>
    <div class="alert alert-light text-center">No posts yet. Click <a href="/compose">Compose</a> to add one.</div>
  <% } else { %>
    <div class="row">
      <% posts.forEach(function(post){ %>
        <div class="col-md-6 col-lg-4 mb-4">
          <div class="card h-100">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title"><%= post.title %></h5>
              <h6 class="card-subtitle mb-2 text-muted small"><%= new Date(post.createdAt).toLocaleString() %></h6>
              <p class="card-text flex-grow-1"><%= post.body.length > 200 ? post.body.slice(0,200) + '...' : post.body %></p>
            </div>
            <div class="card-footer bg-white d-flex justify-content-between">
              <a href="/edit/<%= post.id %>" class="btn btn-sm btn-outline-primary">Edit</a>
              <form action="/delete/<%= post.id %>" method="POST" onsubmit="return confirm('Delete this post?')">
                <button class="btn btn-sm btn-outline-danger">Delete</button>
              </form>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  <% } %>
</main>

<%- include("partials/footer") %>
<!-- Bootstrap JS here -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>